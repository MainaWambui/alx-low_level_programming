#include <stdio.h>

/**
 * main - prints the phrase given using puts "Programming"
 * Return: 0 if exited properly and non zero otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
